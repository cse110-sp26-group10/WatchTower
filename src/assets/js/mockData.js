// Data Engineer: Create mock WatchTower data
// Mock telemetry data (errors, performance, signals, deployments)

/**
 * General Ideals
 * Variables: id, timestamp, type (error/performance/signal), message, deploymentID
 * Deployment: buildID, version, commitHash, deployedAt, environment
 */